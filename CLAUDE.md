@AGENTS.md

# Claude Code — notes

The working agreements above are imported from [AGENTS.md](AGENTS.md), the
single source of truth for **every** agent (Claude Code, Codex, Cursor,
Copilot…). Claude Code only auto-loads `CLAUDE.md`, so this file exists just
to pull `AGENTS.md` in — keep the agreements in `AGENTS.md`, not here.

Nothing Claude-Code-specific overrides them today. Add Claude-only notes
below if the need arises (e.g. "use plan mode for changes under `src/foo/`").
